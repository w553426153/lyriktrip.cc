## Copy to deploy/.env on the server and fill the values.
##
## Domain / TLS
DOMAIN=example.com
LETSENCRYPT_EMAIL=admin@example.com

## API
API_PORT=3000

## Postgres
POSTGRES_DB=lyriktrip
POSTGRES_USER=lyriktrip
POSTGRES_PASSWORD=change_me

## Optional: for seed script (can be same as POSTGRES_* above)
DATABASE_URL=postgres://lyriktrip:change_me@db:5432/lyriktrip

## Feishu webhook proxy (used by the Smart Form -> POST /api/feishu)
## Set these on the server (deploy/.env) so the API can send messages securely.
FEISHU_WEBHOOK_URL=https://www.feishu.cn/flow/api/trigger-webhook/xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
ALLOWED_ORIGINS=https://example.com
